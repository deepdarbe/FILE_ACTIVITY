# FILE ACTIVITY - Windows File Share Analysis & Archiving System
# Python 3.10+
# SQLite3 is built-in - no external DB dependency!

# CLI
click>=8.4.1

# Web Framework
fastapi>=0.136.3
uvicorn[standard]>=0.27.0

# Windows API
pywin32>=311

# Configuration
pyyaml>=6.0.3

# Scheduling
apscheduler>=3.11.2

# Reports
openpyxl>=3.1.0
reportlab>=4.0.0

# Analytics (optional): DuckDB for columnar aggregate queries over SQLite.
# When installed and analytics.enabled=true, heavy reports (duplicates,
# growth, stats) run through DuckDB via the sqlite_scanner extension.
# SQLite remains the source of truth; DuckDB only reads.
duckdb>=1.5.2

# Parquet staging (optional): pyarrow lets the scanner stage rows to
# Parquet files which DuckDB then bulk-INSERTs into SQLite via the
# sqlite_scanner extension. 10-50x faster than per-batch executemany on
# 100k+ row scans. Missing package -> ParquetStager.available=False and
# the scanner silently falls back to bulk_insert_scanned_files.
#
# Floor >=23.0.1 — clears three advisories that the old >=14.0 range
# still allowed a resolver to pick:
#   * GHSA-5wvp-7f3h-6wmm (CRITICAL, <14.0.1) — arbitrary code execution
#     reading a malicious Parquet/IPC file via read_table.
#   * PYSEC-2024-161 (<17.0.0).
#   * GHSA-rgxp-2hwp-jwgg / PYSEC-2026-113 (15.0.0–23.0.0) — use-after-free
#     reading an IPC file with pre-buffering; fixed in 23.0.1.
# We only ever read .parquet files WE wrote to data/staging/ (crash-recovery
# re-ingest in staging.py), never untrusted Arrow IPC, so practical
# exploitability is low — but the floor must not ALLOW a vulnerable resolve.
# 23.x is API-compatible with our read_table/write_table usage.
pyarrow>=23.0.1

# Active Directory lookup (optional): pure-Python LDAP client.
# When installed and active_directory.enabled=true, dashboard resolves
# username -> email/display_name for activity detail + notifications.
# Missing package -> ADLookup.available=False and feature silently disabled.
ldap3>=2.9.1

# Ed25519 signing for the tamper-evident audit chain.
#  * WORM signing (issue #38): when audit.signing_key_path points at an
#    Ed25519 private key, AuditExporter writes a detached <file>.sig next
#    to each JSONL export.
#  * Signed Tree Head export (2026-06-12, Trillian/Rekor pattern): the STH
#    checkpoint {tree_size, root_hash, timestamp, signature, public_key}
#    is Ed25519-signed so external auditors verify the chain with our
#    published public key (src/storage/audit_sth.py, scripts/audit_sth.py).
# Both code paths import cryptography lazily and degrade/skip cleanly if it
# is somehow absent; the test-suite uses importorskip. Pinned as a real dep
# since both features are first-class compliance tooling now.
cryptography>=41.0

# Per-user JWT session tokens (Wave 10 #307).
# Short-lived access tokens (8 h) + long-lived refresh tokens (24 h).
# Secret auto-generated at first start and persisted in SQLite so it
# survives restarts; can be overridden via FILEACTIVITY_SESSION_SECRET.
PyJWT>=2.8.0

# TOTP/MFA second-factor authentication (Wave 10 #311).
# RFC 6238 Time-based One-Time Passwords for per-user enrollment.
# Missing pyotp -> enrolled logins fail CLOSED (no code is accepted); un-enrolled
# users are unaffected (TOTP is opt-in).
pyotp>=2.9.0
# On-box QR rendering for TOTP enrollment (pure-python, zero-dependency). Keeps the
# otpauth:// secret on the server instead of a third-party QR service. Optional:
# if absent, the enrollment UI falls back to manual secret entry.
segno>=1.6.0
